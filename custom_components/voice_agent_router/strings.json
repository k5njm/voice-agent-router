{
  "config": {
    "step": {
      "user": {
        "title": "Voice Agent Router",
        "description": "Configure your LLM-powered voice agent router.",
        "data": {
          "api_key": "OpenRouter API Key",
          "model": "Model"
        },
        "data_description": {
          "api_key": "API key from openrouter.ai — used for cloud LLM fallback.",
          "model": "OpenRouter model identifier (e.g., google/gemini-2.5-flash-preview)."
        }
      }
    },
    "abort": {
      "already_configured": "Voice Agent Router is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Voice Agent Router Options",
        "data": {
          "model": "Model",
          "system_prompt": "System Prompt",
          "temperature": "Temperature",
          "max_tool_iterations": "Max Tool Iterations",
          "enable_local_router": "Enable Local Router",
          "enable_followup": "Enable Follow-up Conversations",
          "followup_timeout": "Follow-up Timeout (seconds)"
        },
        "data_description": {
          "model": "Model identifier to use for routing and responses.",
          "system_prompt": "System prompt sent to the LLM with each conversation turn.",
          "temperature": "Sampling temperature (0.0 - 2.0). Lower is more deterministic.",
          "max_tool_iterations": "Maximum number of tool-call rounds per conversation turn.",
          "enable_local_router": "Use a local classifier to pre-route simple intents before calling the LLM.",
          "enable_followup": "After responding, automatically trigger the satellite to listen again for follow-up commands.",
          "followup_timeout": "How long the satellite waits for a follow-up command before going idle (3-30)."
        }
      }
    }
  }
}
